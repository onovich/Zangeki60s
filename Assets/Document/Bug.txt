1. VFXCore 的帧动画，是按上层输入的帧率控制的，按目前配置是 1/12 秒每帧 -> 目前虽然通过 FixedUpdate 的帧率进行了约束，但是帧动画的下层使用 Update，逻辑使用 FixedUpdate，两边不一致。
   动画没有立刻播（改下层可以修复）；不明原因导致动画帧数比预期慢了4倍（可能是因为使用不同生命周期
2. VFX 播放的时机、伤害的时机，都是按游戏帧控制的，帧率不固定。应该做一个计时器用于同步时机和动画帧率